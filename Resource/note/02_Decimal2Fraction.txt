import math
a=input().split(',')
top=int(a[1]+a[2])-int('0'+a[1])
bottom=int('9'*len(a[2])+'0'*len(a[1]))
d=math.gcd(top,bottom)
top,bottom=top//d,bottom//d
top=int(a[0])*bottom+top
print(top,'/',bottom)